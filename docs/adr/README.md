# Architecture Decision Records

Record durable decisions here using numbered Markdown files, for example `0001-runtime-profile-boundary.md`. The active OpenSpec design is the source of truth while the beta architecture is being built.
