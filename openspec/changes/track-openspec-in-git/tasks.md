## 1. Git tracking

- [x] 1.1 Remove `openspec/` from `.gitignore`; leave `.cursor/` ignored
- [x] 1.2 Ensure `openspec/config.yaml` and this change directory are addable by git

## 2. Contributor docs

- [x] 2.1 Update `CONTRIBUTING.md` so OpenSpec artifacts are committed with the change they describe
- [x] 2.2 Update `.github/pull_request_template.md` to expect OpenSpec in the PR when a change uses it
